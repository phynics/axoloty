included:
    - Source

excluded:
    # Vendored third-party source (Flight-School/AnyCodable) — kept close to
    # upstream for diffability, not reformatted to house style.
    - Source/Common/AnyCodable

disabled_rules:
    - trailing_whitespace
    - todo
    - vertical_parameter_alignment
    - identifier_name
